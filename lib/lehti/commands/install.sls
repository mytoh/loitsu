
(library (lehti commands install)
  (export install)
  (import
    (scheme base)
    (scheme write)
    )

  (begin

    (define (install args)
      (let ((package (caddr args)))




        )

      )

    )
  )
